/**
 * Provides methods to add data to the MetricBase database, to execute
 * transforms on the MetricBase
 * database, and to receive the results of the transforms.
 * @class Client
 * @typedef {Object}  Client
 */
class Client {
    /**
     * Creates an instance of the client class to access the MetricBase database.
     */
    constructor() {}
    /**
     * Save metric data to the MetricBase database.
     * @param {DataBuilder} metricData A DataBuilder object containing metric data.
     * @returns Method does not return a value
     */
    put(metricData) {}
}